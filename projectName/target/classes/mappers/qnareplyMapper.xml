<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.coffeemall.reply">

	<insert id="insert">
		INSERT INTO tbl_qna_reply(qno, replyText, rno) VALUES(#{qno}, #{replyText}, seq_qna_reply_rno.NEXTVAL)
	</insert>

	<select id="reply" resultType="QnAReplyDTO">
		SELECT * FROM tbl_qna_reply WHERE qno = #{qno}
	</select>
	
	<delete id="delete">
		DELETE FROM tbl_qna_reply WHERE rno = #{rno}
	</delete>
	
	<update id="update">
		UPDATE tbl_qna_reply SET replyText = #{replyText} WHERE rno = #{rno}
	</update>
	
</mapper>