<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.coffeemall.qna">

	<insert id="insert">
		INSERT INTO tbl_qna(qno, userid, qTitle, qContent, qWriteDay, qUpdateDay)
		VALUES(seq_qna_qno.NEXTVAL, #{userid}, #{qTitle}, #{qContent}, SYSDATE, SYSDATE)
		
		<selectKey keyProperty="qno" resultType="integer" order="AFTER">
			SELECT seq_qna_qno.CURRVAL FROM DUAL
		</selectKey>
	</insert>
	
	<select id="list" resultType="QnaDTO">
		SELECT * FROM tbl_qna ORDER BY qno ASC
	</select>
	
	<select id="read" resultType="QnaDTO">
		SELECT * FROM tbl_qna WHERE qno = #{qno}
	</select>
	
	<update id="increase">
		UPDATE tbl_qna SET readCnt = readCnt + 1 WHERE qno = #{qno}
	</update>
	
	<delete id="delete">
		DELETE FROM tbl_qna WHERE qno = #{qno}
	</delete>
	
	<select id="updateUI" resultType="QnaDTO">
		SELECT * FROM tbl_qna WHERE qno = #{qno}
	</select>
	
	<update id="update">
		UPDATE tbl_qna SET userid = #{userid}, qTitle = #{qTitle}, qContent = #{qContent}, qUpdateDay = SYSDATE WHERE qno = #{qno}
	</update>
	
	  <select id="getListPaging" resultType="QnaDTO">
    
	    <![CDATA[
	        
	        SELECT qno, qTitle, qContent, userid, qWriteDay, qUpdateDay from(
	        
	                select  rownum  as rn, qno, qTitle, qContent, userid, qWriteDay, qUpdateDay
	                  
	                from tbl_qna where rownum <= #{pageNum} * #{amount}) 
	                
	        where rn > (#{pageNum} -1) * #{amount}
	    
	    ]]>
    
    </select>
    
    <select id="getTotal" resultType="int">
    
        SELECT count(*) FROM tbl_qna
    
    </select>
	
</mapper>